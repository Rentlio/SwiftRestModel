language: objective-c
osx_image: xcode8
before_install:
- gem install cocoapods --pre # Since Travis is not always on latest version
- pod update
script:
- xcodebuild -workspace SwiftRestModel.xcworkspace -scheme SwiftRestModel -sdk iphonesimulator build test